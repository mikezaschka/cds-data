import{_ as o,C as r,o as a,c as n,a2 as t,b as d,w as s,a as l,E as c,a3 as h}from"./chunks/framework.CT6jiXWi.js";const y=JSON.parse('{"title":"Cross-Service Scenarios: $expand and Navigation","description":"","frontmatter":{},"headers":[],"relativePath":"federation/concepts/cross-service-scenarios.md","filePath":"federation/concepts/cross-service-scenarios.md","lastUpdated":1783784358000}'),p={name:"federation/concepts/cross-service-scenarios.md"};function g(m,e,u,k,b,v){const i=r("Mermaid");return a(),n("div",null,[e[1]||(e[1]=t(`<h1 id="cross-service-scenarios-expand-and-navigation" tabindex="-1">Cross-Service Scenarios: <code>$expand</code> and Navigation <a class="header-anchor" href="#cross-service-scenarios-expand-and-navigation" aria-label="Permalink to &quot;Cross-Service Scenarios: \`$expand\` and Navigation&quot;">​</a></h1><p>This is the canonical reference for every <strong>cross-service scenario</strong> the plugin handles: the four <code>$expand</code> topologies and the two navigation flows. For each scenario, we separate <strong>what the CAP runtime provides out of the box</strong> from <strong>what this plugin adds on top</strong> — the exact contract the plugin promises.</p><p>The naming is directional and CAP-aligned. The short labels in code and tests (<code>A</code>, <code>B</code>, <code>C</code>, <code>N1</code>, <code>N2</code>) are kept for now; see <a href="#legend-previous-scenario-labels">Legend</a> at the bottom to map them to the new names.</p><hr><h2 id="how-cap-processes-expand" tabindex="-1">How CAP processes <code>$expand</code> <a class="header-anchor" href="#how-cap-processes-expand" aria-label="Permalink to &quot;How CAP processes \`$expand\`&quot;">​</a></h2><p>CAP does <strong>not</strong> split <code>$expand</code> into separate queries. The OData adapter parses <code>$expand=buyer</code> into a CQN column object with an <code>.expand</code> property, embedded in <code>req.query.SELECT.columns</code>:</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">req.query </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SELECT: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        from: { ref: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Orders&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        columns: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">            &#39;*&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            { ref: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;buyer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], expand: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;*&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ← expand item</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>The handler receives the full query including expand items. CAP expects the handler to return results with the expanded data already nested:</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    orderId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;O001&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    buyer: { ID: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;C001&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Acme Corp&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>If the handler returns flat results (no <code>buyer</code> object), the expand is silently empty. This is the mechanism every scenario below rides on top of.</p><p>Ref: <a href="https://cap.cloud.sap/docs/guides/integration/calesi" target="_blank" rel="noreferrer">Service Integration guide: Navigation &amp; Expands Across Services</a></p><hr><h2 id="topology-at-a-glance" tabindex="-1">Topology at a glance <a class="header-anchor" href="#topology-at-a-glance" aria-label="Permalink to &quot;Topology at a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Scenario</th><th>Main entity</th><th>Expanded entity</th><th>Same provider?</th></tr></thead><tbody><tr><td><a href="#delegated-expand">Delegated expand</a></td><td>remote</td><td>remote</td><td>yes</td></tr><tr><td><a href="#cross-service-expand-local-remote">Cross-service expand: local → remote</a></td><td>local</td><td>remote</td><td>n/a</td></tr><tr><td><a href="#cross-service-expand-remote-local">Cross-service expand: remote → local</a></td><td>remote</td><td>local</td><td>n/a</td></tr><tr><td><a href="#cross-service-expand-cross-provider">Cross-service expand: cross-provider</a></td><td>local</td><td>remote</td><td><strong>no</strong> — different providers per expand item</td></tr><tr><td><a href="#cross-service-navigation-local-remote">Cross-service navigation: local → remote</a></td><td>local (entry)</td><td>remote (tail)</td><td>n/a</td></tr><tr><td><a href="#cross-service-navigation-remote-local">Cross-service navigation: remote → local</a></td><td>remote (entry)</td><td>local (tail)</td><td>n/a</td></tr></tbody></table><hr><h2 id="strategies-and-scenarios" tabindex="-1">Strategies and scenarios <a class="header-anchor" href="#strategies-and-scenarios" aria-label="Permalink to &quot;Strategies and scenarios&quot;">​</a></h2><p>Every cross-service scenario has the <strong>remote side pinned to <code>@federation.delegate</code></strong> — there are no exceptions. The <strong>local side</strong> can be either a plain local CDS entity (stored in <code>cds.db</code>) or a <code>@federation.replicate</code> view. From a query-time perspective these are indistinguishable, because replicate materializes its data into an ordinary local table. As a consequence, <strong><code>@federation.replicate</code> has no scenarios of its own</strong>; it only participates as the local side of a cross-service flow.</p><table tabindex="0"><thead><tr><th>Scenario</th><th>Main entity</th><th>Expand / navigation target</th></tr></thead><tbody><tr><td><a href="#delegated-expand">Delegated expand</a></td><td><code>@federation.delegate</code></td><td><code>@federation.delegate</code> (same provider)</td></tr><tr><td><a href="#cross-service-expand-local-remote">Cross-service expand: local → remote</a></td><td>local <em>or</em> <code>@federation.replicate</code></td><td><code>@federation.delegate</code></td></tr><tr><td><a href="#cross-service-expand-remote-local">Cross-service expand: remote → local</a></td><td><code>@federation.delegate</code></td><td>local <em>or</em> <code>@federation.replicate</code></td></tr><tr><td><a href="#cross-service-expand-cross-provider">Cross-service expand: cross-provider</a></td><td>local <em>or</em> <code>@federation.replicate</code></td><td><code>@federation.delegate</code> (multiple providers)</td></tr><tr><td><a href="#cross-service-navigation-local-remote">Cross-service navigation: local → remote</a></td><td>local <em>or</em> <code>@federation.replicate</code></td><td><code>@federation.delegate</code></td></tr><tr><td><a href="#cross-service-navigation-remote-local">Cross-service navigation: remote → local</a></td><td><code>@federation.delegate</code></td><td>local <em>or</em> <code>@federation.replicate</code></td></tr></tbody></table>`,18)),(a(),d(h,null,{default:s(()=>[c(i,{id:"mermaid-225",class:"mermaid",graph:"flowchart%20LR%0A%20%20subgraph%20strategies%20%5BStrategies%5D%0A%20%20%20%20delegate%5B%22%40federation.delegate%22%5D%0A%20%20%20%20replicate%5B%22%40federation.replicate%22%5D%0A%20%20%20%20localEntity%5B%22plain%20local%20entity%22%5D%0A%20%20end%0A%0A%20%20subgraph%20scenarios%20%5BScenarios%5D%0A%20%20%20%20delegated%5B%22Delegated%20expand%22%5D%0A%20%20%20%20l2r%5B%22Cross-service%20expand%3A%20local%20to%20remote%22%5D%0A%20%20%20%20r2l%5B%22Cross-service%20expand%3A%20remote%20to%20local%22%5D%0A%20%20%20%20cp%5B%22Cross-service%20expand%3A%20cross-provider%22%5D%0A%20%20%20%20navL2R%5B%22Cross-service%20navigation%3A%20local%20to%20remote%22%5D%0A%20%20%20%20navR2L%5B%22Cross-service%20navigation%3A%20remote%20to%20local%22%5D%0A%20%20end%0A%0A%20%20delegate%20--%3E%20delegated%0A%20%20delegate%20--%3E%20l2r%0A%20%20delegate%20--%3E%20r2l%0A%20%20delegate%20--%3E%20cp%0A%20%20delegate%20--%3E%20navL2R%0A%20%20delegate%20--%3E%20navR2L%0A%0A%20%20replicate%20--%3E%7C%22behaves%20as%20local%22%7C%20l2r%0A%20%20replicate%20--%3E%7C%22behaves%20as%20local%22%7C%20r2l%0A%20%20replicate%20--%3E%7C%22behaves%20as%20local%22%7C%20cp%0A%20%20replicate%20--%3E%7C%22behaves%20as%20local%22%7C%20navL2R%0A%20%20replicate%20--%3E%7C%22behaves%20as%20local%22%7C%20navR2L%0A%0A%20%20localEntity%20--%3E%20l2r%0A%20%20localEntity%20--%3E%20r2l%0A%20%20localEntity%20--%3E%20cp%0A%20%20localEntity%20--%3E%20navL2R%0A%20%20localEntity%20--%3E%20navR2L%0A"})]),fallback:s(()=>[...e[0]||(e[0]=[l(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=t(`<hr><p>Each section below follows the same four-part template:</p><ol><li><strong>What it is</strong> — one-paragraph definition, one concrete <code>GET</code> example, one-line topology summary.</li><li><strong>What CAP provides out of the box</strong> — what the CAP runtime does (or fails to do) <em>without this plugin</em>.</li><li><strong>What this plugin adds</strong> — the specific handler, function, or registry entry, with a pointer to the source file.</li><li><strong>Status and test coverage</strong> — <code>supported</code> / <code>partial</code> / <code>planned</code>, with the test ids still used in the <a href="https://github.com/mikezaschka/cds-data/tree/main/packages/cds-data-federation/test/integration" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/</code></a> suite.</li></ol><hr><h2 id="delegated-expand" tabindex="-1">Delegated expand <a class="header-anchor" href="#delegated-expand" aria-label="Permalink to &quot;Delegated expand&quot;">​</a></h2><h3 id="what-it-is" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is" aria-label="Permalink to &quot;What it is&quot;">​</a></h3><p>A <code>$expand</code> whose main entity and the expand target both live on the <strong>same remote provider</strong>. The consumer&#39;s local entities are pure consumption views over that provider.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /consumer/Orders?$expand=buyer</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><code>Orders</code> is <code>@federation.delegate</code> on <code>ProviderService.Orders</code>.</li><li><code>buyer</code> association (renamed from <code>customer</code>) points to <code>ProviderService.Customers</code>.</li><li>Both resolve to the same remote service.</li></ul><p><strong>Topology:</strong> remote → remote (same provider).</p><p><strong>Applies to strategies:</strong> <code>@federation.delegate</code> on both sides (same provider).</p><h3 id="what-cap-provides-out-of-the-box" tabindex="-1">What CAP provides out of the box <a class="header-anchor" href="#what-cap-provides-out-of-the-box" aria-label="Permalink to &quot;What CAP provides out of the box&quot;">​</a></h3><p><strong>Everything.</strong> CAP&#39;s runtime handles this scenario completely via <code>remote.run(req.query)</code> and its <a href="https://cap.cloud.sap/docs/guides/integration/calesi#delegation" target="_blank" rel="noreferrer">Automatic Query Translation</a> along the projection chain:</p><ul><li>Column rename translation in <code>$select</code>, <code>$filter</code>, <code>$orderby</code> (via <code>as</code> clauses in the projection).</li><li>Column restriction to projected fields only.</li><li>Association rename translation in <code>$expand</code> (<code>buyer</code> → <code>customer</code>).</li><li>Forwarding of <code>$expand</code> to the remote service.</li><li>Nested expands, <code>$select</code> within expands, to-many associations.</li><li>Mapping expanded results back through the target entity&#39;s projection.</li></ul><p>Demonstrated in SAP&#39;s <a href="https://github.com/capire/xtravels" target="_blank" rel="noreferrer">xtravels sample</a> with a one-line handler:</p><div class="language-js vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;READ&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Customers, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> s4.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req.query))</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="what-this-plugin-adds" tabindex="-1">What this plugin adds <a class="header-anchor" href="#what-this-plugin-adds" aria-label="Permalink to &quot;What this plugin adds&quot;">​</a></h3><p><strong>Nothing in the read path.</strong> The plugin&#39;s <code>@federation.delegate</code> handler is conceptually the same one-liner; CAP does the work.</p><p>The only value-add is <strong>optional response-level caching</strong> when the entity is annotated with a TTL:</p><div class="language-cds vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">cds</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@federation.delegate: { cache: { ttl: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">60000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">entity Orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> projection</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> on</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ProviderService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Orders</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The cache wraps the outbound <code>remote.run(req.query)</code> call — including the entire <code>$expand</code> payload — via the peer dependency <a href="https://github.com/mikezaschka/cds-caching" target="_blank" rel="noreferrer"><code>cds-caching</code></a> (<code>cache.strategy: &#39;response&#39;</code>, default). <strong><code>cache.strategy: &#39;entity&#39;</code></strong> caches direct delegated-entity READs in SQLite; cross-service expand batch fetches still use the delegate path in the current release. See <a href="./../integration/caching.html">Caching</a>.</p><h3 id="status-and-test-coverage" tabindex="-1">Status and test coverage <a class="header-anchor" href="#status-and-test-coverage" aria-label="Permalink to &quot;Status and test coverage&quot;">​</a></h3><p><strong>Supported.</strong> Covered by tests <code>A1–A7</code> in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/test/integration/delegate/basic.test.js" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/delegate/basic.test.js</code></a> (to-one renames, to-many, multi-expand, nested expand, <code>$expand</code> + <code>$select</code>, single-entity key access).</p><hr><h2 id="cross-service-expand-local-→-remote" tabindex="-1">Cross-service expand: local → remote <a class="header-anchor" href="#cross-service-expand-local-→-remote" aria-label="Permalink to &quot;Cross-service expand: local → remote&quot;">​</a></h2><h3 id="what-it-is-1" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is-1" aria-label="Permalink to &quot;What it is&quot;">​</a></h3><p>The main entity is <strong>local</strong> (stored in the consumer&#39;s DB or another local service) and has an association to a <strong>remote</strong> entity. OData requests expanding that association cross a service boundary that neither service can resolve alone.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /consumer/Reviews?$expand=product</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><code>Reviews</code> is a local entity (persisted via <code>cds.db</code>).</li><li><code>product</code> points to <code>Products</code>, which is <code>@federation.delegate</code> on the remote <code>ProviderService.Products</code>.</li></ul><p><strong>Topology:</strong> local → remote.</p><p><strong>Applies to strategies:</strong> local entity <em>or</em> <code>@federation.replicate</code> as main; <code>@federation.delegate</code> on the expand target.</p><h3 id="what-cap-provides-out-of-the-box-1" tabindex="-1">What CAP provides out of the box <a class="header-anchor" href="#what-cap-provides-out-of-the-box-1" aria-label="Permalink to &quot;What CAP provides out of the box&quot;">​</a></h3><p>The local DB service resolves the main <code>Reviews</code> query, including <code>$filter</code>, <code>$orderby</code>, and <code>$select</code> on local columns. But CAP&#39;s DB service <strong>cannot resolve <code>$expand</code> targets across service boundaries</strong> — its expand handling is implemented as correlated SQL subqueries, which don&#39;t make sense for a remote OData/REST service. The expanded field comes back silently empty.</p><p>No native CAP mechanism collects foreign keys from one service&#39;s result and issues a follow-up query against another service.</p><h3 id="what-this-plugin-adds-1" tabindex="-1">What this plugin adds <a class="header-anchor" href="#what-this-plugin-adds-1" aria-label="Permalink to &quot;What this plugin adds&quot;">​</a></h3><p><code>registerLocalExpandResolvers()</code> registers an <code>after(&#39;READ&#39;)</code> handler on the local entity that implements the <strong>batch-fetch + stitch</strong> pattern:</p><ol><li>Let the local DB read <code>Reviews</code> without the remote expand.</li><li>Inspect the original query for expand items whose target is <code>@federation.delegate</code>.</li><li>Collect distinct foreign key values (<code>product_ID</code>) from the results.</li><li>Issue a single remote call: <code>GET Products?$filter=ID in (&#39;P001&#39;,&#39;P002&#39;,...)</code>.</li><li>Apply <strong>rename mapping via the view mapping registry</strong> (e.g., <code>productId</code>/<code>productName</code> ↔ <code>ID</code>/<code>name</code>).</li><li>Stitch the fetched records back into the main result as nested objects.</li><li>Optionally cache the remote sub-query (the outbound batch-fetch is a cacheable unit).</li></ol><p>Source: <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/expand-local-to-remote.js" target="_blank" rel="noreferrer"><code>srv/delegation/expand-local-to-remote.js</code></a>. Registry is populated by <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/annotation-scanner.js" target="_blank" rel="noreferrer"><code>srv/annotation-scanner.js</code></a> and consumed by <code>resolveFederatedExpand()</code>.</p><h3 id="status-and-test-coverage-1" tabindex="-1">Status and test coverage <a class="header-anchor" href="#status-and-test-coverage-1" aria-label="Permalink to &quot;Status and test coverage&quot;">​</a></h3><p><strong>Supported.</strong> Covered by tests <code>B1–B3</code> in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/test/integration/expand-local-to-remote/cross-service-expand-local-to-remote.test.js" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/expand-local-to-remote/cross-service-expand-local-to-remote.test.js</code></a> (basic local → delegate, multiple local → delegate associations, <code>$select</code> within the remote sub-query).</p><hr><h2 id="cross-service-expand-remote-→-local" tabindex="-1">Cross-service expand: remote → local <a class="header-anchor" href="#cross-service-expand-remote-→-local" aria-label="Permalink to &quot;Cross-service expand: remote → local&quot;">​</a></h2><h3 id="what-it-is-2" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is-2" aria-label="Permalink to &quot;What it is&quot;">​</a></h3><p>The main entity is <strong>remote</strong> (fetched via a <code>@federation.delegate</code> consumption view) and has an association back into a <strong>local</strong> entity — typically a local extension or annotation table owned by the consumer.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /consumer/Customers(&#39;C001&#39;)?$expand=bookmarks</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><code>Customers</code> is <code>@federation.delegate</code> on <code>ProviderService.Customers</code>.</li><li><code>bookmarks</code> is a local association (unmanaged, keyed by <code>customer_ID</code>) to a local <code>Bookmarks</code> table the consumer owns.</li></ul><p><strong>Topology:</strong> remote → local.</p><p><strong>Applies to strategies:</strong> <code>@federation.delegate</code> as main; local entity <em>or</em> <code>@federation.replicate</code> on the expand target.</p><h3 id="what-cap-provides-out-of-the-box-2" tabindex="-1">What CAP provides out of the box <a class="header-anchor" href="#what-cap-provides-out-of-the-box-2" aria-label="Permalink to &quot;What CAP provides out of the box&quot;">​</a></h3><p>The remote main fetch works: CAP runs <code>Customers(&#39;C001&#39;)</code> against <code>ProviderService</code> via the delegate handler. But CAP <strong>cannot expand into a local association from a remote result</strong> — the remote provider has no knowledge of the consumer&#39;s local <code>bookmarks</code> association, and CAP does not automatically issue a follow-up query against the local DB to resolve it.</p><h3 id="what-this-plugin-adds-2" tabindex="-1">What this plugin adds <a class="header-anchor" href="#what-this-plugin-adds-2" aria-label="Permalink to &quot;What this plugin adds&quot;">​</a></h3><p>Three functions in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/expand-remote-to-local.js" target="_blank" rel="noreferrer"><code>srv/delegation/expand-remote-to-local.js</code></a> cooperate:</p><ol><li><code>buildLocalAssocInfo()</code> — inspects the consumption view&#39;s CSN for local (non-federated) association targets.</li><li><code>splitLocalExpands()</code> — before forwarding to the remote, strips local-target expand items out of the query so the remote doesn&#39;t reject them.</li><li><code>resolveRemoteToLocalExpands()</code> — after the remote response lands, queries the local DB by the foreign key(s) on the remote result rows, then stitches the local records into the response.</li></ol><p>The result: from the consumer&#39;s perspective, <code>$expand=bookmarks</code> &quot;just works&quot; on a remote entity.</p><p>Top-level <code>$filter</code> through a local association (e.g. <code>Products?$filter=LocalEntity/Name eq &#39;X&#39;</code>, or <code>contains(LocalEntity/Name,&#39;…&#39;)</code>) is handled by <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/local-navigation-filters.js" target="_blank" rel="noreferrer"><code>resolveLocalNavigationFilters()</code></a>: the plugin queries the local DB first, rewrites the filter to a source-key <code>IN</code> list, then forwards to the remote. Lambda filters on to-many local collections (<code>bookmarks/any(...)</code>) use <code>resolveLocalLambdaFilters()</code>.</p><h3 id="status-and-test-coverage-2" tabindex="-1">Status and test coverage <a class="header-anchor" href="#status-and-test-coverage-2" aria-label="Permalink to &quot;Status and test coverage&quot;">​</a></h3><p><strong>Supported.</strong> Covered by the <code>C</code>-prefixed tests in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/test/integration/expand-remote-to-local/cross-service-expand-remote-to-local.test.js" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/expand-remote-to-local/cross-service-expand-remote-to-local.test.js</code></a> (remote customer with local bookmarks, key and filtered access, navigation-path <code>$filter</code> on local to-one assocs).</p><hr><h2 id="cross-service-expand-cross-provider" tabindex="-1">Cross-service expand: cross-provider <a class="header-anchor" href="#cross-service-expand-cross-provider" aria-label="Permalink to &quot;Cross-service expand: cross-provider&quot;">​</a></h2><h3 id="what-it-is-3" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is-3" aria-label="Permalink to &quot;What it is&quot;">​</a></h3><p>A variant of <strong>local → remote</strong> where a single local entity expands associations that resolve to <strong>different remote providers</strong>. A single delegated query can never cover both, because CAP&#39;s outbound service is fixed per entity.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /consumer/Orders?$expand=buyer,shipment</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><code>Orders</code> is local (or a local view).</li><li><code>buyer</code> → <code>CustomerService.Customers</code> (provider #1).</li><li><code>shipment</code> → <code>LogisticsService.Shipments</code> (provider #2).</li></ul><p><strong>Topology:</strong> local → remote, with expand targets split across multiple providers.</p><p><strong>Applies to strategies:</strong> local entity <em>or</em> <code>@federation.replicate</code> as main; <code>@federation.delegate</code> on multiple different providers.</p><h3 id="what-cap-provides-out-of-the-box-3" tabindex="-1">What CAP provides out of the box <a class="header-anchor" href="#what-cap-provides-out-of-the-box-3" aria-label="Permalink to &quot;What CAP provides out of the box&quot;">​</a></h3><p><strong>Nothing specific to this case.</strong> Two different remotes can never share a single delegated query. CAP doesn&#39;t fail loudly either — each cross-service expand item silently comes back empty for the same reason as plain local → remote.</p><h3 id="what-this-plugin-adds-3" tabindex="-1">What this plugin adds <a class="header-anchor" href="#what-this-plugin-adds-3" aria-label="Permalink to &quot;What this plugin adds&quot;">​</a></h3><p>The same batch-fetch + stitch machinery as local → remote, applied per provider: the resolver splits expanded items by their source service and issues <strong>one remote call per provider</strong>, stitching each result set independently. No extra annotation or configuration is needed — the plugin reads each expanded entity&#39;s <code>@federation.delegate</code> annotation to discover its provider.</p><p>In practice, this is already <strong>implicitly supported</strong> today via the local → remote code path; see the walkthrough in <a href="/cds-data/federation/getting-started/cross-provider-mashup.html">Cross-Provider Mashup</a>. This doc now calls it out as its own case so the contract is explicit.</p><h3 id="status-and-test-coverage-3" tabindex="-1">Status and test coverage <a class="header-anchor" href="#status-and-test-coverage-3" aria-label="Permalink to &quot;Status and test coverage&quot;">​</a></h3><p><strong>Supported, not separately tested.</strong> No dedicated <code>CP</code>-prefixed tests exist yet; the scenario currently rides on top of the <code>B</code>-series machinery. <strong>Gap to address in the follow-up rename/test pass</strong> — we should add a dedicated test case for <code>$expand</code> across two providers so regressions surface clearly.</p><hr><h2 id="cross-service-navigation-local-→-remote" tabindex="-1">Cross-service navigation: local → remote <a class="header-anchor" href="#cross-service-navigation-local-→-remote" aria-label="Permalink to &quot;Cross-service navigation: local → remote&quot;">​</a></h2><h3 id="what-it-is-4" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is-4" aria-label="Permalink to &quot;What it is&quot;">​</a></h3><p>An OData path expression that traverses an association from a <strong>local</strong> entry entity into a <strong>remote</strong> target entity.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /consumer/Customers(&#39;C001&#39;)/orders</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><code>Customers</code> is local (possibly a projection), <code>orders</code> points to <code>Orders</code> which is <code>@federation.delegate</code>.</li><li>Equivalent CQN: <code>SELECT from Orders where customer_ID = &#39;C001&#39;</code>.</li></ul><p>Distinct from <code>$expand</code>: the path expression <em>is the query</em>, not a sub-request nested inside a larger result.</p><p><strong>Applies to strategies:</strong> local entity <em>or</em> <code>@federation.replicate</code> as entry; <code>@federation.delegate</code> on the target.</p><h3 id="what-cap-provides-out-of-the-box-4" tabindex="-1">What CAP provides out of the box <a class="header-anchor" href="#what-cap-provides-out-of-the-box-4" aria-label="Permalink to &quot;What CAP provides out of the box&quot;">​</a></h3><p>CAP resolves navigation paths within a single service only. When the navigation target lives behind a different service, CAP does not automatically rewrite the path into a filter on the target service.</p><h3 id="what-this-plugin-adds-4" tabindex="-1">What this plugin adds <a class="header-anchor" href="#what-this-plugin-adds-4" aria-label="Permalink to &quot;What this plugin adds&quot;">​</a></h3><p><code>resolveLocalToRemoteNavigation()</code> in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/cross-service-navigation.js" target="_blank" rel="noreferrer"><code>srv/delegation/cross-service-navigation.js</code></a> rewrites the navigation path at the entry point:</p><ol><li>Detect the navigation segment whose target is remote-backed.</li><li>Resolve the key(s) of the local entry (<code>Customers(&#39;C001&#39;)</code>) against the local DB if the entry is persistent, or against the upstream provider if the entry is itself a remote view.</li><li>Translate the association into a <code>$filter</code> on the remote target using the registered key mapping (e.g., <code>customer_ID eq &#39;C001&#39;</code>).</li><li>Delegate the resulting query as a standard <code>@federation.delegate</code> READ.</li></ol><h3 id="status-and-test-coverage-4" tabindex="-1">Status and test coverage <a class="header-anchor" href="#status-and-test-coverage-4" aria-label="Permalink to &quot;Status and test coverage&quot;">​</a></h3><p><strong>Supported.</strong> Covered by tests labelled <code>N1</code> (and related cases <code>N3</code>, <code>N4</code>) in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/test/integration/navigation/cross-service-navigation.test.js" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/navigation/cross-service-navigation.test.js</code></a>. Sequence diagrams and gotchas in <a href="/cds-data/federation/getting-started/joining-local-with-remote.html">Joining Local with Remote</a>.</p><hr><h2 id="cross-service-navigation-remote-→-local" tabindex="-1">Cross-service navigation: remote → local <a class="header-anchor" href="#cross-service-navigation-remote-→-local" aria-label="Permalink to &quot;Cross-service navigation: remote → local&quot;">​</a></h2><h3 id="what-it-is-5" tabindex="-1">What it is <a class="header-anchor" href="#what-it-is-5" aria-label="Permalink to &quot;What it is&quot;">​</a></h3><p>The inverse of the previous scenario: the entry entity is <strong>remote</strong>, and the navigation tail points at a <strong>local</strong> association.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /consumer/Orders(&#39;O001&#39;)/bookmarks</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><code>Orders</code> is <code>@federation.delegate</code> on <code>ProviderService.Orders</code>.</li><li><code>bookmarks</code> is a local unmanaged association on the consumer side, keyed by <code>order_ID</code>.</li></ul><p><strong>Applies to strategies:</strong> <code>@federation.delegate</code> as entry; local entity <em>or</em> <code>@federation.replicate</code> on the target.</p><h3 id="what-cap-provides-out-of-the-box-5" tabindex="-1">What CAP provides out of the box <a class="header-anchor" href="#what-cap-provides-out-of-the-box-5" aria-label="Permalink to &quot;What CAP provides out of the box&quot;">​</a></h3><p>The same limitation as local → remote: CAP handles navigation only within one service. A path that starts on a remote entity and ends on a local association is not rewritten automatically.</p><h3 id="what-this-plugin-adds-5" tabindex="-1">What this plugin adds <a class="header-anchor" href="#what-this-plugin-adds-5" aria-label="Permalink to &quot;What this plugin adds&quot;">​</a></h3><p><code>rewriteRemoteToLocalNavigation()</code> in the same <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/cross-service-navigation.js" target="_blank" rel="noreferrer"><code>srv/delegation/cross-service-navigation.js</code></a> handles the mirror-image rewrite:</p><ol><li>Fetch the remote entry (<code>Orders(&#39;O001&#39;)</code>) via the standard delegate path.</li><li>Extract the foreign key values needed to resolve the local association.</li><li>Issue a local query: <code>SELECT from Bookmarks where order_ID = &#39;O001&#39;</code>.</li><li>Return the local result set as the response body.</li></ol><h3 id="status-and-test-coverage-5" tabindex="-1">Status and test coverage <a class="header-anchor" href="#status-and-test-coverage-5" aria-label="Permalink to &quot;Status and test coverage&quot;">​</a></h3><p><strong>Supported.</strong> Covered by tests labelled <code>N2</code> (and related case <code>N5</code>) in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/test/integration/navigation/cross-service-navigation.test.js" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/navigation/cross-service-navigation.test.js</code></a>. Walkthrough in <a href="/cds-data/federation/getting-started/extending-remote-with-local.html">Extending Remote with Local</a>.</p><hr><h2 id="view-mapping-registry" tabindex="-1">View Mapping Registry <a class="header-anchor" href="#view-mapping-registry" aria-label="Permalink to &quot;View Mapping Registry&quot;">​</a></h2><p>The registry is populated by <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/annotation-scanner.js" target="_blank" rel="noreferrer"><code>srv/annotation-scanner.js</code></a> during CAP bootstrap and consumed by the batch-fetch resolvers. It exists <strong>only to support cross-service expand: local → remote (and cross-provider)</strong> — the one scenario where CAP&#39;s automatic query translation can&#39;t help because the translation straddles two services.</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Built during annotation scanning, stored globally</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> viewMappingRegistry</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;consumer.Customers&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { isWildcard: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, localToRemote: {}, remoteToLocal: {} },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;consumer.Products&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        isWildcard: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        projectedColumns: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;category&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;price&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;currency&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        localToRemote: { productId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, productName: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, unitPrice: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;price&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        remoteToLocal: { ID: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;productId&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;productName&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, price: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;unitPrice&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &#39;consumer.Orders&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        isWildcard: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        projectedColumns: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;customer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;quantity&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;total&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;status&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderDate&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;modifiedAt&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        localToRemote: { orderId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ID&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, buyer: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;customer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, item: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;product&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, amount: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;total&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, placedOn: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderDate&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        remoteToLocal: { ID: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;orderId&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, customer: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;buyer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, product: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;item&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, total: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;amount&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, orderDate: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;placedOn&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br></div></div><p>For the other scenarios the registry is not read: delegated expand and delegated navigation ride on CAP&#39;s native projection-chain translation; remote → local expand/navigation don&#39;t need outbound field renames because the second hop goes to the local DB which already speaks local names.</p><p><strong>Known limitation:</strong> When multiple consumer entities project the same remote source (e.g., both <code>Customers</code> and <code>Suppliers</code> project <code>ProviderService.Customers</code>), the registry also stores entries keyed by source name — and the last registration wins. Entity-full-name keys remain distinct and are preferred for lookups.</p><hr><h2 id="test-matrix" tabindex="-1">Test matrix <a class="header-anchor" href="#test-matrix" aria-label="Permalink to &quot;Test matrix&quot;">​</a></h2><p>The <code>A</code>, <code>B</code>, <code>C</code>, <code>N1</code>, <code>N2</code> ids below are the short test identifiers used in the <a href="https://github.com/mikezaschka/cds-data/tree/main/packages/cds-data-federation/test/integration" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/</code></a> suite and the <code>.http</code> files under <a href="https://github.com/mikezaschka/cds-data/tree/main/examples" target="_blank" rel="noreferrer"><code>examples/</code></a>. Prose uses the canonical directional names from the <a href="#legend-previous-scenario-labels">Legend</a>.</p><h3 id="delegated-expand-a" tabindex="-1">Delegated expand (A) <a class="header-anchor" href="#delegated-expand-a" aria-label="Permalink to &quot;Delegated expand (A)&quot;">​</a></h3><p>All entities are <code>@federation.delegate</code> on the same remote provider.</p><table tabindex="0"><thead><tr><th>#</th><th>Test</th><th>Request</th><th>Status</th><th>Validates</th></tr></thead><tbody><tr><td>A1</td><td>Orders → buyer (to-one, renamed assoc)</td><td><code>Orders?$expand=buyer</code></td><td><strong>passing</strong></td><td>CAP translates buyer → customer, maps result back</td></tr><tr><td>A2</td><td>Orders → item (to-one, renamed assoc + renamed fields)</td><td><code>Orders?$expand=item</code></td><td><strong>passing</strong></td><td>CAP translates item → product, maps inner fields</td></tr><tr><td>A3</td><td>Customers → orders (to-many)</td><td><code>Customers(&#39;C001&#39;)?$expand=orders</code></td><td><strong>passing</strong></td><td>Array of orders nested under customer</td></tr><tr><td>A4</td><td>Orders → buyer,item (multiple expands)</td><td><code>Orders?$expand=buyer,item</code></td><td><strong>passing</strong></td><td>Both associations expanded in a single request</td></tr><tr><td>A5</td><td>Nested expand</td><td><code>Orders(&#39;O001&#39;)?$expand=buyer($expand=orders)</code></td><td><strong>passing</strong></td><td>Recursive: order.buyer.orders</td></tr><tr><td>A6</td><td>$expand with $select</td><td><code>Orders?$expand=buyer($select=ID,name)</code></td><td><strong>passing</strong></td><td>Only selected fields in expanded data</td></tr><tr><td>A7</td><td>Single entity with expand</td><td><code>Orders(&#39;O001&#39;)?$expand=buyer</code></td><td><strong>passing</strong></td><td>Single order with expanded buyer</td></tr></tbody></table><h3 id="cross-service-expand-local-→-remote-b" tabindex="-1">Cross-service expand: local → remote (B) <a class="header-anchor" href="#cross-service-expand-local-→-remote-b" aria-label="Permalink to &quot;Cross-service expand: local → remote (B)&quot;">​</a></h3><p>Main entity is local, expanded entity is remote / <code>@federation.delegate</code>.</p><table tabindex="0"><thead><tr><th>#</th><th>Test</th><th>Request</th><th>Status</th><th>Validates</th></tr></thead><tbody><tr><td>B1</td><td>Reviews → product (local → delegate)</td><td><code>Reviews?$expand=product</code></td><td><strong>passing</strong></td><td>Remote product data stitched into local reviews</td></tr><tr><td>B2</td><td>Bookmarks → customer (local → delegate)</td><td><code>Bookmarks?$expand=customer</code></td><td><strong>passing</strong></td><td>Remote customer data fetched and stitched</td></tr><tr><td>B3</td><td>Reviews → product with $select</td><td><code>Reviews?$expand=product($select=productId,productName)</code></td><td><strong>passing</strong></td><td>Only selected fields fetched from remote</td></tr></tbody></table><h3 id="cross-service-expand-remote-→-local-c" tabindex="-1">Cross-service expand: remote → local (C) <a class="header-anchor" href="#cross-service-expand-remote-→-local-c" aria-label="Permalink to &quot;Cross-service expand: remote → local (C)&quot;">​</a></h3><p>Main entity is remote / <code>@federation.delegate</code>, expanded entity is local.</p><p>See the <code>C</code>-prefixed tests in <a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/test/integration/expand-remote-to-local/cross-service-expand-remote-to-local.test.js" target="_blank" rel="noreferrer"><code>packages/cds-data-federation/test/integration/expand-remote-to-local/cross-service-expand-remote-to-local.test.js</code></a>.</p><h3 id="cross-service-navigation-n1-n2" tabindex="-1">Cross-service navigation (N1 / N2) <a class="header-anchor" href="#cross-service-navigation-n1-n2" aria-label="Permalink to &quot;Cross-service navigation (N1 / N2)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>#</th><th>Direction</th><th>Example path</th><th>Status</th></tr></thead><tbody><tr><td>N1</td><td>local → remote</td><td><code>Customers(&#39;C001&#39;)/orders</code></td><td><strong>passing</strong></td></tr><tr><td>N2</td><td>remote → local</td><td><code>Orders(&#39;O001&#39;)/bookmarks</code></td><td><strong>passing</strong></td></tr><tr><td>N3–N5</td><td>related cases</td><td>see test file</td><td><strong>passing</strong></td></tr></tbody></table><hr><h2 id="implementation-status" tabindex="-1">Implementation status <a class="header-anchor" href="#implementation-status" aria-label="Permalink to &quot;Implementation status&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Scenario</th><th>Strategies</th><th>Status</th><th>Source</th></tr></thead><tbody><tr><td>Delegated expand</td><td><code>@federation.delegate</code> ↔ <code>@federation.delegate</code> (same provider)</td><td><strong>Done</strong> — CAP handles natively via <code>remote.run(req.query)</code></td><td>(no plugin code)</td></tr><tr><td>Cross-service expand: local → remote</td><td>local / <code>@federation.replicate</code> ↔ <code>@federation.delegate</code></td><td><strong>Done</strong></td><td><a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/expand-local-to-remote.js" target="_blank" rel="noreferrer"><code>srv/delegation/expand-local-to-remote.js</code></a></td></tr><tr><td>Cross-service expand: remote → local</td><td><code>@federation.delegate</code> ↔ local / <code>@federation.replicate</code></td><td><strong>Done</strong></td><td><a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/expand-remote-to-local.js" target="_blank" rel="noreferrer"><code>srv/delegation/expand-remote-to-local.js</code></a></td></tr><tr><td>Cross-service expand: cross-provider</td><td>local / <code>@federation.replicate</code> ↔ <code>@federation.delegate</code> (multiple providers)</td><td><strong>Supported, not separately tested</strong></td><td>(rides on local → remote)</td></tr><tr><td>Cross-service navigation: local → remote</td><td>local / <code>@federation.replicate</code> ↔ <code>@federation.delegate</code></td><td><strong>Done</strong></td><td><a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/cross-service-navigation.js" target="_blank" rel="noreferrer"><code>srv/delegation/cross-service-navigation.js</code></a></td></tr><tr><td>Cross-service navigation: remote → local</td><td><code>@federation.delegate</code> ↔ local / <code>@federation.replicate</code></td><td><strong>Done</strong></td><td><a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/delegation/cross-service-navigation.js" target="_blank" rel="noreferrer"><code>srv/delegation/cross-service-navigation.js</code></a></td></tr><tr><td>View mapping registry</td><td>— (infrastructure, used only by cross-service expand: local → remote and cross-provider)</td><td><strong>Done</strong></td><td><a href="https://github.com/mikezaschka/cds-data/blob/main/packages/cds-data-federation/srv/annotation-scanner.js" target="_blank" rel="noreferrer"><code>srv/annotation-scanner.js</code></a></td></tr><tr><td>Response-level caching for delegated expand</td><td><code>@federation.delegate</code></td><td><strong>Done</strong> — via <a href="https://github.com/mikezaschka/cds-caching" target="_blank" rel="noreferrer"><code>cds-caching</code></a></td><td>(peer dependency)</td></tr></tbody></table><hr><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><p>Task-focused walkthroughs that demonstrate these scenarios in practice:</p><ul><li><a href="/cds-data/federation/getting-started/joining-local-with-remote.html">Joining Local with Remote</a> — cross-service expand: local → remote and cross-service navigation: local → remote, with sequence diagrams.</li><li><a href="/cds-data/federation/getting-started/extending-remote-with-local.html">Extending Remote with Local</a> — cross-service expand: remote → local and cross-service navigation: remote → local.</li><li><a href="/cds-data/federation/getting-started/cross-provider-mashup.html">Cross-Provider Mashup</a> — cross-service expand: cross-provider, resolved by one batch-fetch per provider.</li><li><a href="./terminology.html">Terminology</a> — the <code>&#39;Calesi&#39;</code> definition and how this plugin&#39;s terms relate to CAP&#39;s.</li></ul><hr><h2 id="legend-previous-scenario-labels" tabindex="-1">Legend: previous scenario labels <a class="header-anchor" href="#legend-previous-scenario-labels" aria-label="Permalink to &quot;Legend: previous scenario labels&quot;">​</a></h2><p>Short test IDs (<code>A</code>, <code>B</code>, <code>C</code>, <code>N1</code>, <code>N2</code>) remain in code and tests for traceability. The mapping to canonical names:</p><table tabindex="0"><thead><tr><th>Previous</th><th>New</th></tr></thead><tbody><tr><td>Scenario A</td><td>Delegated expand</td></tr><tr><td>Scenario B</td><td>Cross-service expand: local → remote</td></tr><tr><td>Scenario C</td><td>Cross-service expand: remote → local</td></tr><tr><td>(sub-case of B)</td><td>Cross-service expand: cross-provider</td></tr><tr><td>N1</td><td>Cross-service navigation: local → remote</td></tr><tr><td>N2</td><td>Cross-service navigation: remote → local</td></tr></tbody></table>`,132))])}const f=o(p,[["render",g]]);export{y as __pageData,f as default};
