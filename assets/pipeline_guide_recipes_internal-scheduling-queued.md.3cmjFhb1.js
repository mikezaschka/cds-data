import{_ as s,o as i,c as a,a2 as n}from"./chunks/framework.CT6jiXWi.js";const u=JSON.parse('{"title":"Internal scheduling with the queued engine","description":"","frontmatter":{},"headers":[],"relativePath":"pipeline/guide/recipes/internal-scheduling-queued.md","filePath":"pipeline/guide/recipes/internal-scheduling-queued.md","lastUpdated":1783849762000}'),t={name:"pipeline/guide/recipes/internal-scheduling-queued.md"};function l(r,e,d,h,p,o){return i(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="internal-scheduling-with-the-queued-engine" tabindex="-1">Internal scheduling with the queued engine <a class="header-anchor" href="#internal-scheduling-with-the-queued-engine" aria-label="Permalink to &quot;Internal scheduling with the queued engine&quot;">​</a></h1><p><strong>When to pick this recipe:</strong> you want a schedule owned by the CAP app (not an external service), you run the app with more than one instance, and you want the schedule to survive restarts, retry on failure, and fire <strong>only once per tick across all instances</strong>. This is the right upgrade from <code>schedule: &lt;ms&gt;</code> for production-grade deployments that don&#39;t justify the operational overhead of an external scheduler.</p><p>The queued engine is backed by CAP&#39;s persistent task queue: the schedule message is stored in <code>cds.outbox.Messages</code> and dispatched exactly once per interval.</p><div class="warning custom-block"><p class="custom-block-title">Alpha API</p><p>The underlying <code>.schedule()</code> / <code>.every()</code> API is marked experimental in the <a href="https://cap.cloud.sap/docs/node.js/queue#task-scheduling" target="_blank" rel="noreferrer">CAP task scheduling docs</a>. Semantics may still change. For workloads that can&#39;t absorb churn, prefer the default <code>spawn</code> engine or an external trigger.</p></div><h2 id="engines-at-a-glance" tabindex="-1">Engines at a glance <a class="header-anchor" href="#engines-at-a-glance" aria-label="Permalink to &quot;Engines at a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Engine</th><th>Cross-instance single-winner</th><th>Persistent across restarts</th><th>Retry + dead-letter</th><th>Requires outbox table</th></tr></thead><tbody><tr><td><code>spawn</code> (default)</td><td>No — each instance fires</td><td>No</td><td>No</td><td>No</td></tr><tr><td><code>queued</code> (opt-in)</td><td>Yes</td><td>Yes</td><td>Yes</td><td>Yes</td></tr><tr><td>External trigger (JSS / CronJob)</td><td>Yes</td><td>Yes</td><td>Managed by the scheduler</td><td>No</td></tr></tbody></table><p>If you only run a single app instance and tolerate best-effort scheduling, <code>spawn</code> is fine. If you run more than one instance and want predictable cadence, pick <code>queued</code> or an external trigger.</p><h2 id="_1-ensure-the-outbox-table-is-deployed" tabindex="-1">1. Ensure the outbox table is deployed <a class="header-anchor" href="#_1-ensure-the-outbox-table-is-deployed" aria-label="Permalink to &quot;1. Ensure the outbox table is deployed&quot;">​</a></h2><p>The queued engine stores messages in <code>cds.outbox.Messages</code>, shipped as part of <code>@sap/cds/srv/outbox</code>. In a standard CAP project layout (<code>db/</code>, <code>srv/</code>, <code>app/</code>) this is picked up automatically by <code>cds build</code>. If you override <code>requires.db.model</code> in <code>package.json</code>, add the outbox model explicitly:</p><div class="language-jsonc vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;cds&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;requires&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;db&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;kind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hana&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;model&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;db&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;srv&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@sap/cds/srv/outbox&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>For HANA HDI, the table is deployed by your HDI container alongside the rest of the CDS model — no extra steps beyond including the model path.</p><p>For SQLite / local dev, <code>cds deploy</code> picks it up automatically. If you see <em>&quot;Messages table not found&quot;</em> at runtime, the most common cause is an overridden <code>model</code> that drops the outbox path — see the CAP docs <a href="https://cap.cloud.sap/docs/node.js/queue#messages-table-not-found" target="_blank" rel="noreferrer">Messages Table Not Found</a>.</p><h2 id="_2-register-with-engine-queued" tabindex="-1">2. Register with <code>engine: &#39;queued&#39;</code> <a class="header-anchor" href="#_2-register-with-engine-queued" aria-label="Permalink to &quot;2. Register with \`engine: &#39;queued&#39;\`&quot;">​</a></h2><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;@sap/cds&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> pipelines</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cds.connect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">to</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;data-pipeline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pipelines.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addPipeline</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;BusinessPartners&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        source: { service: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;API_BUSINESS_PARTNER&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, entity: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;A_BusinessPartner&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        target: { entity: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;db.BusinessPartners&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        delta: { field: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;modifiedAt&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, mode: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;timestamp&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        schedule: { every: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;10m&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, engine: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;queued&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>The <code>schedule</code> field accepts three shapes:</p><ul><li><code>schedule: 600000</code> — milliseconds (backwards-compatible, implicit <code>engine: &#39;spawn&#39;</code>).</li><li><code>schedule: { every: 600000, engine: &#39;spawn&#39; }</code> — explicit default.</li><li><code>schedule: { every: &#39;10m&#39;, engine: &#39;queued&#39; }</code> — persistent task queue. <code>every</code> accepts the same duration strings as <code>cds.queued().schedule().every()</code> (<code>&#39;1min&#39;</code>, <code>&#39;10m&#39;</code>, <code>&#39;1h&#39;</code>, ...) as well as millisecond numbers.</li></ul><h2 id="_3-how-every-behaves" tabindex="-1">3. How <code>every</code> behaves <a class="header-anchor" href="#_3-how-every-behaves" aria-label="Permalink to &quot;3. How \`every\` behaves&quot;">​</a></h2><p>The queued engine&#39;s <code>.every(&#39;10m&#39;)</code> means <strong>10 minutes after each processing completes</strong>, not every 10 minutes on the wall clock. Implications:</p><ul><li>If a pipeline run takes 3 minutes, the next tick fires 10 minutes after that completion (13 minutes wall-clock).</li><li>Overlapping fires are impossible — the task runner will not enqueue the next tick until the previous one finishes.</li><li>This differs from <code>engine: &#39;spawn&#39;</code> (<code>cds.spawn({ every })</code>), which fires on fixed wall-clock intervals regardless of run duration. The tracker&#39;s concurrency guard protects against overlap, but you still pay for wasted fires.</li></ul><p>For most data pipelines &quot;every 10 min after the last run&quot; is the desired semantic — no thundering herd, no wasted queries.</p><h2 id="_4-inspect-failed-runs" tabindex="-1">4. Inspect failed runs <a class="header-anchor" href="#_4-inspect-failed-runs" aria-label="Permalink to &quot;4. Inspect failed runs&quot;">​</a></h2><p>When the CAP task runner exhausts its retry budget (<code>maxAttempts</code>, default 20, configurable via <code>cds.requires.queue.maxAttempts</code>), the schedule message lands in the dead-letter queue — it stays in <code>cds.outbox.Messages</code> with <code>attempts &gt;= maxAttempts</code>.</p><p>Independently, every pipeline run produces a <code>PipelineRuns</code> row via the usual tracker path. So you have two complementary views:</p><ul><li><code>GET /pipeline/PipelineRuns?$filter=status eq &#39;failed&#39;&amp;$orderby=startTime desc</code> — the pipeline-level view. Each scheduled attempt shows <code>trigger=&#39;scheduled&#39;</code>.</li><li>A dead-letter queue inspection service over <code>cds.outbox.Messages</code> — the scheduling-level view. Useful if the pipeline itself never ran (e.g. the service couldn&#39;t be connected). See the CAP docs on <a href="https://cap.cloud.sap/docs/node.js/queue#managing-the-dead-letter-queue" target="_blank" rel="noreferrer">Managing the Dead Letter Queue</a>.</li></ul><h2 id="_5-ad-hoc-queued-runs" tabindex="-1">5. Ad-hoc queued runs <a class="header-anchor" href="#_5-ad-hoc-queued-runs" aria-label="Permalink to &quot;5. Ad-hoc queued runs&quot;">​</a></h2><p>The same queued engine is available for one-off runs through the programmatic API, without registering a <code>schedule</code>:</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> pipelines</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cds.connect.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">to</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;data-pipeline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pipelines.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;BusinessPartners&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, { async: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, engine: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;queued&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Semantics mirror the scheduled path — the run is enqueued via <code>cds.queued(srv).emit(&#39;PIPELINE.TICK&#39;, ...)</code> and dispatched by a single app instance. Useful when you want cross-instance single-winner semantics for a manually triggered run (e.g. a BTP JSS-fired OData action that must not double-fire across replicas).</p><p>The returned envelope omits <code>done</code> because the run may execute on another instance — there&#39;s no in-process completion signal. Subscribe via <code>after(&#39;PIPELINE.DONE&#39;, name, ...)</code> for notifications, or poll <code>PipelineRuns</code> for the terminal state.</p><h2 id="_6-coexistence" tabindex="-1">6. Coexistence <a class="header-anchor" href="#_6-coexistence" aria-label="Permalink to &quot;6. Coexistence&quot;">​</a></h2><ul><li><strong>Mixing engines per pipeline is allowed.</strong> One pipeline can use <code>engine: &#39;spawn&#39;</code>, another <code>engine: &#39;queued&#39;</code>, and a third can be externally triggered (no <code>schedule</code>).</li><li><strong>Don&#39;t mix internal and external on the same pipeline.</strong> Setting <code>schedule</code> on a pipeline that an external scheduler also calls results in double-firing. The concurrency guard prevents duplicate work but wastes resources.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./external-scheduling-jss.html">External scheduling with SAP BTP Job Scheduling Service</a> — when centralized BTP-native cron is the better fit.</li><li><a href="./../../reference/management-service.html">Reference → Management Service</a> — the <code>execute</code> action JSS and scripts call, plus the <code>RunTrigger</code> enum.</li><li><a href="https://cap.cloud.sap/docs/node.js/queue" target="_blank" rel="noreferrer">CAP docs: Queueing with <code>cds.queued</code></a> — task queue configuration (<code>maxAttempts</code>, <code>timeout</code>, <code>legacyLocking</code>, ...).</li></ul>`,33)])])}const k=s(t,[["render",l]]);export{u as __pageData,k as default};
