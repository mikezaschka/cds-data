import{c as t,Q as a,j as s,m as i}from"./chunks/framework.DCQwiej8.js";const k=JSON.parse('{"title":"Terminology","description":"","frontmatter":{},"headers":[],"relativePath":"pipeline/guide/concepts/terminology.md","filePath":"pipeline/guide/concepts/terminology.md","lastUpdated":1783588917000}'),r={name:"pipeline/guide/concepts/terminology.md"};function n(o,e,d,l,h,p){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="terminology" tabindex="-1">Terminology <a class="header-anchor" href="#terminology" aria-label="Permalink to &quot;Terminology&quot;">​</a></h1><p>The vocabulary <code>cds-data-pipeline</code> uses. For how these engine terms relate to the federation and materialization layers, see the <a href="/cds-data/concepts/terminology.html">shared Terminology</a>.</p><hr><h2 id="pipeline" tabindex="-1">Pipeline <a class="header-anchor" href="#pipeline" aria-label="Permalink to &quot;Pipeline&quot;">​</a></h2><p>A <strong>pipeline</strong> is a linear <code>READ → MAP → WRITE</code> job between exactly one source and one target, with built-in tracker, retry, concurrency guard, and event hooks. Pipelines are registered programmatically via <code>cds.connect.to(&#39;DataPipelineService&#39;).addPipeline({ ... })</code> and scheduled (periodic) or triggered (one-shot).</p><p>Pipeline behaviour — row-preserving copy vs. aggregated snapshot vs. cross-service move — is <strong>inferred from the config shape</strong>. See <a href="./inference.html">Inference rules</a> for the full table and the configs that are rejected at registration. The three recipe names (<strong>replicate</strong>, <strong>materialize</strong>, <strong>move-to-service</strong>) are doc-level use-case labels — they describe common combinations of read shape and target destination, but they are not a field on <code>addPipeline(...)</code> or a column on the tracker.</p><hr><h2 id="source-and-target" tabindex="-1">Source and target <a class="header-anchor" href="#source-and-target" aria-label="Permalink to &quot;Source and target&quot;">​</a></h2><p>Every pipeline has <strong>one source</strong> and <strong>one target</strong>.</p><ul><li><strong>Source</strong> — a service (identified by a <code>cds.requires</code> key) plus an entity name, optionally with a CQN <code>query</code> closure that replaces entity-based reading with a custom SELECT. Source adapters bridge the service&#39;s transport (OData V4 / V2, REST, or a custom adapter) to a uniform streaming contract the plugin consumes.</li><li><strong>Target</strong> — a service plus an entity name, typically a DB entity (<code>@cds.persistence.table</code>). The WRITE phase is dispatched through a <a href="./../targets/">target adapter</a>: the built-in <a href="./../targets/db.html"><code>DbTargetAdapter</code></a> handles the common case (<code>target.service</code> unset or <code>&#39;db&#39;</code>); the built-in <a href="./../targets/odata.html"><code>ODataTargetAdapter</code></a> handles remote OData services; everything else is covered by a <a href="./../targets/custom.html">custom target adapter</a> or a <a href="./../recipes/event-hooks.html#on-as-a-target-adapter-alternative"><code>PIPELINE.WRITE_BATCH</code> event hook</a>. For replicate pipelines the target is most idiomatically a <strong>consumption view</strong> — a local <code>projection on &lt;remote.Entity&gt;</code> that also carries the column selection and rename mapping (see <a href="./consumption-views.html">Consumption views</a>).</li></ul><hr><h2 id="mode" tabindex="-1">Mode <a class="header-anchor" href="#mode" aria-label="Permalink to &quot;Mode&quot;">​</a></h2><p><strong>Mode</strong> controls how the pipeline decides what to read and what to write.</p><table tabindex="0"><thead><tr><th>Mode</th><th>Read</th><th>Write</th></tr></thead><tbody><tr><td><code>full</code></td><td>Read all rows from source.</td><td>Truncate target, then upsert fresh rows. Multi-source-aware truncate preserves rows from other pipelines writing to the same target.</td></tr><tr><td><code>delta</code></td><td>Read only rows changed since the last successful run (using the source adapter&#39;s delta strategy).</td><td>Upsert the delta batch. No truncate.</td></tr><tr><td><code>partial-refresh</code></td><td>Read a query-shaped slice of rows.</td><td>Replace the slice, leave other rows alone.</td></tr></tbody></table><p>Entity-shape pipelines default to <code>delta</code>; query-shape pipelines default to <code>full</code>. See <a href="./inference.html">Inference rules</a> for the full defaults table.</p><hr><h2 id="delta-strategy" tabindex="-1">Delta strategy <a class="header-anchor" href="#delta-strategy" aria-label="Permalink to &quot;Delta strategy&quot;">​</a></h2><p>How the source adapter discovers changes. The engine hands the adapter a <strong>tracker</strong> carrying the last known watermark; the adapter returns all rows newer than that watermark.</p><table tabindex="0"><thead><tr><th>Strategy</th><th>Watermark</th><th>Adapter responsibility</th></tr></thead><tbody><tr><td><strong>timestamp</strong></td><td><code>lastSync</code> timestamp</td><td>Issue a <code>$filter=modifiedAt gt &lt;lastSync&gt;</code> (or REST equivalent). Adapter chooses the comparison field via options.</td></tr><tr><td><strong>key-based</strong></td><td><code>lastKey</code> primary key value</td><td>Issue a filter/pagination anchored on the sort key so rows past the anchor are returned in order.</td></tr><tr><td><strong>datetime-fields</strong></td><td>Per-field timestamps in a composite watermark</td><td>For sources exposing multiple independently updated timestamps.</td></tr></tbody></table><p>All three are implemented for OData V4 and V2. REST supports <code>timestamp</code> via configurable <code>delta</code> query params.</p><hr><h2 id="tracker" tabindex="-1">Tracker <a class="header-anchor" href="#tracker" aria-label="Permalink to &quot;Tracker&quot;">​</a></h2><p><code>Pipelines</code> and <code>PipelineRuns</code> are the two tracker entities exposed by the management OData service.</p><ul><li><strong>Pipelines</strong> — one row per registered pipeline. Carries source / target references, current <code>mode</code>, <code>lastSync</code>, <code>lastKey</code>, cumulative statistics, and status. Behaviour is inferred from the current config at registration (see <a href="./inference.html">Inference rules</a>).</li><li><strong>PipelineRuns</strong> — one row per invocation. Carries start / end timestamps, trigger type, per-phase statistics (<code>created</code> / <code>updated</code> / <code>deleted</code> / <code>skipped</code>), mode, and any error context for failed runs.</li></ul><p>See the <a href="./../../reference/management-service.html">Management Service reference</a> for the OData shape and available actions.</p><hr><h2 id="event-namespace" tabindex="-1">Event namespace <a class="header-anchor" href="#event-namespace" aria-label="Permalink to &quot;Event namespace&quot;">​</a></h2><p>Pipeline event hooks use the <code>PIPELINE.*</code> namespace. Five events bracket each run:</p><table tabindex="0"><thead><tr><th>Event</th><th>Cardinality</th><th>When it fires</th></tr></thead><tbody><tr><td><code>PIPELINE.START</code></td><td>once</td><td>After the concurrency guard acquires the tracker lock, before READ. Run-level setup, veto, trace correlation.</td></tr><tr><td><code>PIPELINE.READ</code></td><td>once</td><td>Around the READ phase. Default handler resolves the source adapter and produces an async iterable (<code>sourceStream</code>) the engine iterates.</td></tr><tr><td><code>PIPELINE.MAP_BATCH</code></td><td>per batch</td><td>Around the transformation phase between read and write. Field renames are applied here via <code>config.viewMapping.remoteToLocal</code> when supplied by the caller; user <code>PIPELINE.MAP_BATCH</code> hooks can override or extend the mapping.</td></tr><tr><td><code>PIPELINE.WRITE_BATCH</code></td><td>per batch</td><td>Around the UPSERT (or adapter-specific write) into the target entity.</td></tr><tr><td><code>PIPELINE.DONE</code></td><td>once</td><td>After the tracker row is finalized. Fires on both success and failure; <code>req.data.status</code> discriminates. Canonical hook for end-of-run notifications.</td></tr></tbody></table><p>Every payload carries <code>runId</code> so handlers can correlate across phases; <code>MAP_BATCH</code> and <code>WRITE_BATCH</code> additionally carry <code>batchIndex</code> (0-based). Register handlers via the standard CAP pattern:</p><div class="language-javascript vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">srv.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">before</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;PIPELINE.WRITE_BATCH&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;MyPipeline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">srv.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;PIPELINE.MAP_BATCH&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;MyPipeline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">next</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> next</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rows.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> r.active);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">srv.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">after</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;PIPELINE.DONE&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;MyPipeline&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">_results</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* notify */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div>`,31)])])}const g=t(r,[["render",n]]);export{k as __pageData,g as default};
