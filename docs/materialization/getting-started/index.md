# Getting started

Short path from a clean CAP app to a working `@materialize.snapshot` entity.

| Page | What you'll do |
|---|---|
| [Installation](installation.md) | Install the plugin and peer dependencies. |
| [First snapshot](first-snapshot.md) | Declare a `group by` projection and run a full-refresh materialize pipeline. |

## Related

- [Built-in materialize (programmatic)](/pipeline/guide/recipes/built-in-materialize) — same engine without annotations
- [Stage then aggregate](../concepts/stage-then-aggregate.md) — combine with `@federation.replicate`
