# Contributor canonical concepts

These pages are the **authoritative** concept reference for contributors and AI assistants. Consumer-facing subsets are published under:

- [`docs/pipeline/guide/concepts/`](../../docs/pipeline/guide/concepts/) — engine scope
- [`docs/federation/concepts/`](../../docs/federation/concepts/) — federation scope

Do not duplicate facts across `spec/concepts/` and `docs/` beyond intentional pruning; cross-link from public pages where needed.
