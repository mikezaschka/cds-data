{
  "name": "cds-data-materialization",
  "version": "0.1.0",
  "description": "Declarative @materialize.snapshot annotations for scheduled aggregate snapshots on SAP CAP, composing cds-data-pipeline.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikezaschka/cds-data-monorepo.git",
    "directory": "packages/cds-data-materialization"
  },
  "keywords": [
    "sap",
    "cap",
    "cds",
    "materialization",
    "aggregation",
    "snapshot"
  ],
  "author": "Mike Zaschka <opensource@mikezaschka.com>",
  "license": "MIT",
  "main": "cds-plugin.js",
  "files": [
    "srv/**",
    "index.cds",
    "cds-plugin.js",
    "README.md",
    "AGENTS.md",
    "skills/**"
  ],
  "peerDependencies": {
    "@sap/cds": ">=8",
    "cds-data-pipeline": ">=0.1.0"
  },
  "peerDependenciesMeta": {
    "cds-caching": {
      "optional": true
    }
  },
  "scripts": {
    "test": "jest --config jest.config.js --runInBand --forceExit",
    "test:unit": "jest --config jest.config.js --runInBand --forceExit --testPathPattern=test/unit",
    "test:integration": "jest --config jest.config.js --runInBand --forceExit --testPathPattern=test/integration"
  },
  "devDependencies": {
    "@cap-js/cds-test": "^0.4.1",
    "@cap-js/sqlite": "^2",
    "cds-data-pipeline": "*",
    "chai": "^4.5.0",
    "chai-as-promised": "^7.1.2",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=22"
  }
}
