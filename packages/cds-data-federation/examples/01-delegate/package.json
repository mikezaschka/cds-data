{
  "name": "federation-example-01-delegate",
  "version": "0.1.0",
  "private": true,
  "description": "Runnable example: @federation.delegate — live proxy, renames, static where, CUD opt-in (OData V4)",
  "scripts": {
    "start": "cds-serve --port 4131"
  },
  "dependencies": {
    "@sap-cloud-sdk/http-client": "^4",
    "@sap-cloud-sdk/resilience": "^4",
    "@sap/cds": ">=9",
    "cds-data-federation": "*",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": ">=2"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": { "url": ":memory:" }
      },
      "ProviderService": {
        "kind": "odata",
        "model": "srv/external/ProviderService",
        "credentials": { "url": "http://localhost:4141/odata/v4/provider" }
      }
    }
  }
}
